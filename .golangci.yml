version: "2"

linters:
  exclusions:
    rules:
      # Error checking on setup/teardown calls in tests is not worth the noise.
      - path: _test\.go
        linters:
          - errcheck

formatters:
  enable:
    - gofmt
